import React from "react";

const Filter = (props) => {
  const styles = {
    border: "1px solid rgba(0, 0, 0, 0.5)",
    marginLeft: "100px",
    marginRight: "1600px",
    marginBottom: "50px",
    paddingLeft: "15px",
    height: "50px",
    textAlign: "left",
  };

  return (
    <div style={styles}>
      <h3>Filter</h3>
    </div>
  );
};

export default Filter;
